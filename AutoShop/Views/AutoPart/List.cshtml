@model AutoShop.ViewModels.AutoPartListViewModel
<body>
    <h1>@Model.CurrentWorkOrder</h1>
    @foreach (var parts in Model.auparts)
            {
                <div>
                    <h2>@parts.Name</h2>
                    <h3>@parts.Price.ToString("c")</h3>
                    
                    
                </div>
            }
</body>
